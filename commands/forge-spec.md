# Forge Spec (local)

**No usar The Forge API.**

1. Paso 0 debe estar en verde.
2. Redactar `docs/sdd/spec.md` (qué/por qué, sin stack; RF → D-IDs).
3. Resolver `[NEEDS CLARIFICATION]` antes de MDD.
4. `WORKFLOW.yaml`: `phase: spec`, `gates.spec.status: passed`.

Siguiente: `/forge-mdd`.
